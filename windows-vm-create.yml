- name: Deploy Windows Servers
  hosts: all
  gather_facts: False
  connection: local
  become: no
  vars:
    ansible_port: 5986
    instance_wait_connection_timeout: 800
  vars_files:
    - win-nodes-vmware.yml
    
  roles:
  #- deploy
  - ansible-role-vmware