---
nodes:
  - name: windows
    role: windows
    app_name: windows_demo
    flavor: m1.medium
    image: Windows Server 2016 v2
    key_name: ansible-oatakan
    volume_size: 40
    nics:
      - net-name: private_network
    user_data: "{{ lookup('template', 'templates/userdata.txt.j2') }}"
